language: node_js
node_js:
- '11'
install:
- yarn global add node-gyp
- yarn global add gulp
- yarn
cache: yarn
script: gulp build
deploy:
  provider: pages
  skip_cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master
  local-dir: dist
